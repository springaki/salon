%h1 Listing journals

%table
  %thead
    %tr
      %th Checkin at
      %th Karte no
      %th Quantity
      %th Item
      %th
      %th
      %th

  %tbody
    - @journals.each do |journal|
      %tr
        %td= journal.checkin_at
        %td= journal.karte_no
        %td= journal.item.name
        %td= journal.item.price
        %td= journal.quantity
        %td= journal.item.price * journal.quantity
        %td= link_to 'Show', journal
        %td= link_to 'Edit', edit_journal_path(journal)
        %td= link_to 'Destroy', journal, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Journal', new_journal_path
